# Core HTTP and Async
aiohttp

# HTML Parsing (optional - graceful fallback if not installed)
beautifulsoup4

# JavaScript-enabled crawling (optional)
playwright

# Progress bars (optional - scanner works without it)
tqdm

